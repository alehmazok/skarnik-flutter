<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="by.mazokaleh.skarnik">
    <!-- Required to fetch data from the internet. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Needed so canLaunchUrl() can detect Huawei AppGallery for review-fallback deeplink. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="appmarket"/>
        </intent>
    </queries>

    <application
            android:label="@string/AppName"
            android:name="${applicationName}"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round">
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:launchMode="singleTop"
                android:theme="@style/LaunchTheme"
                android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                android:hardwareAccelerated="true"
                android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                    android:name="io.flutter.embedding.android.NormalTheme"
                    android:resource="@style/NormalTheme"
            />
            <!-- App owns deep-link handling via app_links + HandleAppLinkUseCase (regex → SkarnikAppBloc).
                 Flutter's default raw-URI forwarding would otherwise push the full https:// URL into
                 go_router as initial route on cold start, which has no matching route → crashes to error page. -->
            <meta-data
                    android:name="flutter_deeplinking_enabled"
                    android:value="false"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="http"/>
                <data android:scheme="https"/>

                <data android:host="${hostname}"/>
                <data android:host="www.${hostname}"/>

                <data android:host="${appHostname}"/>
                <data android:host="www.${appHostname}"/>

                <data android:pathPrefix="/belrus/"/>
                <data android:pathPrefix="/rusbel/"/>
                <data android:pathPrefix="/tsbm/"/>
                <data android:pathPrefix="/r/"/>
            </intent-filter>
        </activity>
        <receiver
                android:name=".widget.WordOfDayWidgetReceiver"
                android:exported="true"
                android:label="@string/word_of_day_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                    android:name="android.appwidget.provider"
                    android:resource="@xml/word_of_day_widget_info"/>
        </receiver>
        <!-- Не лагаваць у аналітыку пераходы па экранах -->
        <meta-data android:name="google_analytics_automatic_screen_reporting_enabled" android:value="false"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
                android:name="flutterEmbedding"
                android:value="2"/>
    </application>
</manifest>
