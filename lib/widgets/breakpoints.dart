/// Material 3 window-size-class breakpoints used for tablet layout support.
abstract class Breakpoints {
  /// Width at which [NavigationRail] replaces the bottom [NavigationBar].
  static const rail = 600.0;

  /// Width at which list+detail split-pane layout replaces single-pane push navigation.
  static const masterDetail = 840.0;
}
